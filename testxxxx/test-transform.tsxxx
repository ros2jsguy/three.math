/**
 * @author wayne parrott, wayneparrott.com
 */

'use strict';

import { assert } from 'chai';

import { Matrix3 } from '../../lib/math/matrix3';
import { Matrix4 } from '../../lib/math/matrix4';
import { Quaternion } from '../../lib/math/quaternion';
import { Transform } from '../../lib/math/transform';
import { Vector3 } from '../../lib/math/vector3';
import {
	x,
	y,
	z,
	zero3,
	one3,
	two3
} from './constants.tests';



describe('Transform Tests', () => {

  it( "Transform Vector", () => {

  let transform = new Transform();
  transform.setRotation(new Quaternion(1, 0, 0, 0));
  
  let v = new Vector3(10, 20, 30);
  transform.transformVector3(v);

  console.log('transform: ', v);
  });

});


